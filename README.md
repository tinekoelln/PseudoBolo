# PseudoBolo
Package to interactively create pseudobolometric lightcurves of supernovae with multi-band photometry

## Documentation

Full docs: https://pseudobolo.readthedocs.io/en/latest/

To install the package for developdevelopment, run: pip install -r requirements-dev.txt
To install the package for use only, run pip install -r requirements.txt